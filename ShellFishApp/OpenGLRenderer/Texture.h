#pragma once
#include "opengl.h"
namespace Graphics
{
	/*class Texture
	{
		Texture(int width, int height, GLuint type, bool repeat = false);

		GLuint create();
		void subdata();
		void bind();
		void unbind();

	private:
		bool mRepeat = false;
		int mWidth, mHeight;
		GLuint mType; /// GL_TEXTURE_1D, GL_TEXTURE_2D, etc
		GLuint mTexID;
	};*/
}